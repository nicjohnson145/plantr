version: "3"

tasks:
  build-controller:
    desc: build the controller binary
    cmds:
    - go build -o plantr-controller ./cmd/controller

  run-controller:
    desc: run controller locally
    cmds:
    - task: build-controller
    - ./plantr-controller